# ============================================================================
# LINKEDIN EXTENSION BACKEND - ENVIRONMENT VARIABLES
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values (never commit .env to git!)
# 3. For Render.com deployment, add these in the Environment tab
#
# ============================================================================

# ----------------------------------------------------------------------------
# OpenRouter API Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Get your API key from: https://openrouter.ai/keys
# This is used for AI message customization
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ----------------------------------------------------------------------------
# API Security (REQUIRED)
# ----------------------------------------------------------------------------
# Secret key for authenticating extension requests
# Generate a strong random string (e.g., use: openssl rand -hex 32)
API_SECRET=your_secure_api_secret_here

# ----------------------------------------------------------------------------
# Server Configuration (OPTIONAL - Render sets these automatically)
# ----------------------------------------------------------------------------
# Port to run the server on (Render will override this with PORT=10000)
PORT=3000

# Host to bind to (MUST be 0.0.0.0 for Render.com)
HOST=0.0.0.0

# Node environment (development, production, test)
NODE_ENV=production

# ----------------------------------------------------------------------------
# CORS Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Chrome extension ID for CORS (update after publishing extension)
# Format: chrome-extension://YOUR_EXTENSION_ID
# Example: chrome-extension://abcdefghijklmnopqrstuvwxyz123456
ALLOWED_ORIGINS=chrome-extension://your_extension_id_here

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
#
# For Render.com:
# 1. Go to your service dashboard → Environment tab
# 2. Add these environment variables:
#    - OPENROUTER_API_KEY (paste your actual key)
#    - API_SECRET (generate secure random string)
#    - NODE_ENV=production
#
# 3. Render automatically sets:
#    - PORT=10000 (don't override this)
#    - Other system variables
#
# 4. NEVER commit your .env file to git!
#    - The .env file is in .gitignore
#    - Only commit this .env.example file
#
# ============================================================================
